{
  "name": "greek-parsing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build:standalone": "esbuild src/app.js --bundle --format=iife --target=es2018 --external:./lexicon-data.js --outfile=src/app-standalone.js",
    "generate:opengnt": "node scripts/generate-opengnt.js",
    "generate:opengnt-differences": "node scripts/generate-opengnt-na28-differences.js",
    "dev": "python3 -m http.server 5173"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
